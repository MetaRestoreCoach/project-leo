// This file is intentionally empty — OAuth callbacks are now handled
// in index.tsx via the ?code= query parameter (PKCE flow).
// Kept to avoid stale import errors; safe to delete.
export { default } from './index';
